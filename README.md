# Cozy Content Planner

A local-first Instagram content planner for `@cottage.core.designs`.

## Run locally

```bash
npm install
npm run dev
```

## Data storage

Planner data is stored in the browser with `localStorage` under the key `cozy-content-planner:v1`. No API keys or environment variables are required.
